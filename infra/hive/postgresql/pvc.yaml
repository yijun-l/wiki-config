apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: hive
spec:
  storageClassName: local-path
  accessModes: [ ReadWriteOnce ]
  resources:
    requests:
      storage: 1Gi
